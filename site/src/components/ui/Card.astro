---
export interface Props {
  class?: string;
  as?: 'div' | 'article' | 'section' | 'aside';
}

const { class: extraClass = '', as: Tag = 'div' } = Astro.props;
---

<Tag
  class={`bg-bg-elevated border border-border rounded-lg p-6 ${extraClass}`}
>
  <slot />
</Tag>
