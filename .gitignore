# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Node / Astro
node_modules/
site/dist/
site/.astro/

# Env and secrets
.env
.env.*.local
pipeline/.env

# Logs
pipeline/logs/
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Build / deploy
.vercel
dist/

# Local Claude Code (machine-specific)
.claude/settings.local.json

# Database — only ignore SQLite transient sidecar files; the .db itself is versioned
data/*.db-journal
data/*.db-wal
data/*.db-shm

# keep graph outputs, skip heavy/local-only files

# optional: commit for shared extraction speed, skip to keep repo small
graphify-out/cache/

# mtime-based, invalid after git clone - always gitignore this
graphify-out/manifest.json

# local token tracking, not useful to share
graphify-out/cost.json